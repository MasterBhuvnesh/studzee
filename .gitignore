node_modules/

# Local agent and editor state. Machine specific, never committed.
.claude/

# Scratch logs from running a dev server in the background.
.dev.log
.dev.err

# Agent execution scratch: ledgers, task briefs, review packages. The git
# history is the durable record, so none of this belongs in the repository.
.superpowers/
