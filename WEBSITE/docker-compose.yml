services:
  studzee-website:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: studzee-website
    ports:
      - '3000:3000'
    env_file:
      - .env.local
    restart: always
    stdin_open: true
    tty: true
