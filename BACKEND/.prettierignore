node_modules
dist
coverage
.env

# Generated by `npm run ai:reindex` from whatever is actually in the vector
# store. Reformatting it would only mean fmt:check failing after every
# reindex, since the generator writes it the same way each time.
src/services/ai/kb/KB-CONTENTS.md
