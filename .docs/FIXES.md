# FIXES

Log of problems faced and how they were fixed, so any person or agent can avoid repeating them. Add a row whenever a problem is resolved and commit it with the change.

| PROBLEM                                                                                                                          | FIX                                                                                                                                                                                                                                            | CAUSE / OCCUR                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | DATE       |
| -------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| The `KbChunk` migration declared `vector(1024)`, which no embedding the configured model produces would ever fit | Called the embeddings endpoint once and read the length off the response, then set the column, `AI_EMBED_DIM` and the docs to 2048 | The dimension was written from a plausible default rather than measured, because Docker was down and the migration could not be applied to catch it. `nvidia/nemotron-3-embed-1b` returns 2048. Prisma cannot select an `Unsupported` column, so the mismatch would have surfaced as an opaque Postgres error on every insert rather than as a validation failure. Occurs whenever a vector column dimension is assumed instead of measured | 29-08-2026 |
| The planned `hnsw (embedding vector_cosine_ops)` index could not be created at all | Dropped the index. An exact scan over a few dozen chunks is already sub millisecond, and the migration records `halfvec(2048)` as the upgrade path | pgvector caps an HNSW index at 2000 dimensions and the embeddings are 2048, so the statement is rejected outright rather than degraded. `halfvec` raises the cap to 4000 but needs the column type changed, which is a migration not a config edit. Occurs on any pgvector HNSW index above 2000 dimensions | 29-08-2026 |
| Generated study content came back full of em dashes, which house style forbids anywhere | Added `normalisePunctuation` to `stripModelScaffolding` in `client.ts`, rewriting a dash used as punctuation into a comma on the raw reply before it is parsed | The prompt says not to use them and the model did anyway, on the very first live generation. A prompt is a request, not a constraint, so the rule needed enforcing in code. Doing it on the raw string rather than per field means one pass covers every generator and every support answer, and none of those characters are structural in JSON so parsing is unaffected. Occurs on any model output held to a house style rule | 29-08-2026 |
| `nvidia/nemotron-3-ultra-550b-a55b` returned `503 Service temporarily overloaded` on every attempt over half an hour | Verified the pipeline against `nvidia/nemotron-3-super-120b-a12b` instead, which is the same family and needs only an `AI_MODEL` change | The model id is valid and appears in `GET /v1/models`, so this is capacity on the build tier rather than a configuration fault. `AI_BASE_URL` and `AI_MODEL` being the only provider coupling is what made the substitution a one line change. Occurs on the free NVIDIA build tier at peak | 29-08-2026 |
| The Levels tab marked every rung at or below the caller's points as Current, so a user on 150 points saw Current on both Novice and Apprentice | Render the ladder from the new `allLevels` field and take the current rung from `level.key`, which the server already resolves | `LevelCard` computed `isCurrent` as `points >= level.minPoints && nextBoundary > level.minPoints`. `nextBoundary` is the next threshold above the caller's points, which is above every reached rung, not just the highest one, so the second half of the condition was true whenever the first was. Occurs wherever a client rederives a ladder position the server already returns | 26-08-2026 |
| The badge detail bottom sheet rendered Week Warrior as Week, dropping the trailing word | Give the title `w-full text-center` instead of letting it shrink to fit | The title was the only `Text` in that sheet without `text-center`, so inside an `items-center` column it was measured shrink to fit. Android rounds that measurement down and drops the last word that no longer fits, with no ellipsis to show for it. The sibling description and the celebration modal title both carry `text-center` and were never affected. Occurs on any multi word `Text` left shrink to fit inside a centred column on Android | 26-08-2026 |
| Both buttons in the mobile settings Lottie diagnostics played the same minimal probe, so `celebrate.json` was never actually tested | Hold the required `AnimationObject` in state instead of a `'celebrate' | 'minimal'` discriminator, and drop the ternary that chose the source | `playLottie` was declared to take the discriminator but both call sites passed the `require`d module. `require` returns `any`, so `tsc` accepted it, the strict equality against `'celebrate'` was always false, and the fallback branch rendered the probe under both buttons. Every conclusion drawn from the diagnostics for a day was about the wrong file. Occurs wherever a `require`d asset is passed to a narrowly typed parameter | 26-08-2026 |
| The celebration Lottie never played on Android, and switching from `autoPlay` to a ref driven `reset()` plus `play()` did not help | Return to `autoPlay` and delete the ref and its effect | In `LottieAnimationViewManagerImpl.kt` the `play` command with no frame arguments takes the `withCustomFrames == false` branch and calls `LottieDrawable.resumeAnimation()`, whereas the `autoPlay` prop reaches `playAnimation()`. Resuming a composition that has never started is not equivalent to playing it. Occurs on any ref driven `play()` with no explicit start and end frame | 26-08-2026 |
| A day of Lottie debugging produced no diagnostic signal at all | Wire `onAnimationFailure` and `onAnimationLoaded` to the logger on every `LottieView` | Neither prop had a handler anywhere in the app. The native side dispatches `onAnimationFailure` when a composition fails to parse, so a bad JSON payload was failing silently and looked identical to a missing native module. Occurs on any `LottieView` mounted without a failure handler | 26-08-2026 |
| TypeScript reported that `baseUrl` is deprecated and will stop functioning in TypeScript 7                                       | Added `ignoreDeprecations: "6.0"` and aligned the backend compiler dependency with TypeScript 6                                                                                                                                                | The editor was using TypeScript 6 while the project still installed TypeScript 5, which rejected the TypeScript 6 suppression value during the project typecheck. Occurs when the editor and project compiler versions diverge                                                                                                                                                                                                                                                                                                                                                                                                                                         | 25-08-2026 |
| The profile and achievements screens requested `GET /progress/me` in an infinite loop, tripping the global rate limiter          | Read Clerk's `getToken` through a ref, emptied the `useCallback` dependencies, and added an in flight guard                                                                                                                                    | `fetchProgress` was keyed on `getToken`, which Clerk does not keep referentially stable. Each fetch's own state updates re-rendered the screen, rebuilt the callback, and refired the effect that called it, so the loop fed itself. This is the same root cause as the notification registration loop fixed on 20-08-2026, resurfacing on every new screen that keys an effect on Clerk callbacks. Occurs on any effect whose dependency is a Clerk hook return value                                                                                                                                                                                                 | 25-08-2026 |
| The achievements screen crashed on the Levels tab with `Couldn't find a navigation context`                                      | Made the segmented tab class sets static per state instead of toggling `shadow-sm` between renders                                                                                                                                             | The crash was NativeWind's warning printer, not navigation: toggling a shadow class after first render trips the css-interop late upgrade path, and `printUpgradeWarning` runs `JSON.stringify` over the component's captured props. That stringify walked into `NavigationStateContext`'s default object, whose `getKey` is a throwing getter, so the warning printer itself threw the navigation error and masked the real warning. Any dynamic class that changes a component's upgrade state (shadow, animated props, View to Pressable) after initial render can reproduce this. Occurs on conditionally toggled style classes that alter component upgrade state | 25-08-2026 |
| View All on the home Machine Learning section showed an empty list while the home screen itself showed content                   | Backfilled the missing `topic` and `tags` fields on existing documents and made the sample seeder always backfill                                                                                                                              | The four original Machine Learning documents were inserted before the `topic` field existed, and Mongoose defaults only apply at insert time, so the server side `topic=machine-learning` filter matched nothing. The home screen hid the bug because its client side grouping falls back to machine-learning when the field is absent. Occurs whenever a new indexed field is added to a collection that already holds production documents                                                                                                                                                                                                                           | 25-08-2026 |
| ESLint reported 4012 problems, almost all `Delete CR`, and `fmt:check` could not be made a CI gate                               | Added `.gitattributes` with `* text=auto eol=lf`, then ran `prettier --write`. 4012 problems became 1                                                                                                                                          | The index already stored LF. The checkout produced the CRLF: `core.autocrlf=true` rewrites the working tree on Windows while Prettier defaults to `endOfLine: "lf"`, so every line of 68 files was flagged. Suppressing it in the Prettier config was rejected, because CI checks out on Linux and the maintainer works on Windows, so the two would still disagree. Occurs on every checkout on Windows                                                                                                                                                                                                                                                               | 14-08-2026 |
| The project carried two Clerk SDKs and two copies of `@clerk/backend`                                                            | Import `clerkClient` from `@clerk/express`, which re-exports it, and drop `@clerk/clerk-sdk-node`. `@clerk/backend` 2.x is now an explicit direct dependency for its types                                                                     | `@clerk/express` supplied the middleware while the end of life `@clerk/clerk-sdk-node` supplied `clerkClient` for the admin role lookup. The latter pinned `@clerk/backend` 0.38 at the top level while express used a nested 2.x, so the version resolved by a bare import depended on hoisting. Occurs on any install                                                                                                                                                                                                                                                                                                                                                | 14-08-2026 |
| After moving to `@clerk/backend` v2, every `req.auth().userId` failed to compile                                                 | Typed the Express augmentation as `SessionAuthObject` rather than `AuthObject`                                                                                                                                                                 | In v2 `AuthObject` widened to a union including machine tokens, which carry no `userId`. `SessionAuthObject` is the `SignedInAuthObject \| SignedOutAuthObject` union this API actually receives. Removing `src/types/express.d.ts` entirely does not work either: `@clerk/express` declares `Request.auth` in module scope, which does not reach the global Express namespace. Occurs on any `@clerk/backend` v1 to v2 move                                                                                                                                                                                                                                           | 14-08-2026 |
| `API.md` still failed `prettier --check` immediately after `prettier --write`                                                    | Ran the write a second time                                                                                                                                                                                                                    | Prettier is not idempotent on that file, so one pass does not reach a fixed point. Occurs whenever that file is reformatted                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 14-08-2026 |
| Six new test files passed under Vitest but failed to compile                                                                     | Gave the `beforeEach` hooks a block body instead of an expression body, and declared the mock argument type on `vi.fn` rather than as an unused parameter                                                                                      | `beforeEach(() => vi.clearAllMocks())` returns `VitestUtils`, which is not assignable to the hook cleanup type, and a `vi.fn` with no declared argument produces an empty tuple that cannot be indexed. Vitest transpiles without typechecking, so all 172 tests were green while `tsc` reported 16 errors. This is the third time the typecheck gate has caught something the suite could not. Occurs on any new test file                                                                                                                                                                                                                                            | 14-08-2026 |
| The admin gate was only ever proven in the allow direction                                                                       | Created a throwaway Clerk user with no role, minted a real token for it, confirmed 403 on every admin route, and deleted the user                                                                                                              | The probe user in `clerk-auth-demo` carries `publicMetadata.role = "admin"`, so driving the admin routes with it returns 200 and proves only half the check. A 200 there initially looked like an auth bypass and was not one. Occurs whenever an auth check is verified with a single privileged identity                                                                                                                                                                                                                                                                                                                                                             | 14-08-2026 |
| The DESKTOP module could not be released at all                                                                                  | `release.sh` validates against `VALID_SERVICES="backend mobile desktop"`, matched by word rather than a regex, and DESKTOP gained the `do-release` scripts it never had                                                                        | The script's regex was `^(notification\|backend\|mobile\|website)$`. It was never updated when DESKTOP joined the tree, and still accepted `notification` and `website`, which were removed on 10-08-2026. Passing either produced a confusing "could not navigate" error rather than a validation failure. Occurs on any desktop release attempt                                                                                                                                                                                                                                                                                                                      | 14-08-2026 |
| The release script could tag a version taken from a dependency entry                                                             | Read the version with `node -p "require('./package.json').version"`                                                                                                                                                                            | It used `grep -oP '"version": "\K[^"]+'`, which returns the first match in the file. That is the package version only as long as no dependency block precedes it. A wrong read produces a wrong git tag, which is what the CI pipeline keys off. Occurs on any manifest where the ordering differs                                                                                                                                                                                                                                                                                                                                                                     | 14-08-2026 |
| `make seed`, `make logs` and `make refresh-cache` failed with `service "api" is not running`                                     | `seed` and `refresh-cache` run on the host; `logs` became `api-logs` and passes `--profile api`                                                                                                                                                | They ran `docker-compose exec api ...` against a compose service that did not exist until 14-08-2026. `seed` and `refresh-cache` could not have worked even with the container, because both go through `ts-node`, a devDependency the production image omits. Occurred on every use since the Makefile was written                                                                                                                                                                                                                                                                                                                                                    | 14-08-2026 |
| Every other Makefile target failed on a machine without the `docker-compose` v1 binary                                           | Rewrote them to use `docker compose`, the Compose v2 plugin subcommand                                                                                                                                                                         | The Makefile predates the v1 to v2 transition. Docker Desktop ships the plugin, not the standalone binary, so `docker-compose` is simply absent. Occurs on any current Docker Desktop install                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 14-08-2026 |
| The containerized API had to be started by hand with a long `docker run` line quoting the network, the env file and the port map | Declared an `api` service in `BACKEND/docker-compose.yml` building the `production` target, and start it with `docker compose --profile api up -d`                                                                                             | The compose file was infrastructure only, so the one component the repository actually builds was the one not described in it. The invocation lived in a chat transcript rather than in the file, so it was not reproducible by anyone else. Occurs every time the container is started                                                                                                                                                                                                                                                                                                                                                                                | 14-08-2026 |
| Adding the API to compose would take port 4000 from the host `npm run dev` workflow                                              | Put the service behind the `api` compose profile, so a plain `docker compose up -d` still starts infrastructure only. Published port is `${API_PORT:-4000}`                                                                                    | Both modes bind 4000 and neither is wrong, so the fix is to make them mutually exclusive on purpose rather than let one silently win. Without the profile the container claims the port at every `up` and `npm run dev` then fails with `EADDRINUSE`. Occurs on every `docker compose up -d`                                                                                                                                                                                                                                                                                                                                                                           | 14-08-2026 |
| `README.md` stated the Vitest suite had never been run on this machine and should be treated as unproven                         | Rewrote the testing section: the suite is at 90 passing across 11 files, and the section now documents the three CI gates, why the typecheck uses the base tsconfig, and the two load bearing defaults in `globalSetup.ts`                     | The Defender esbuild quarantine was resolved on 13-08-2026 and the suite was greened the same day, but the readme was not updated with it. A reader following the readme would have discounted real coverage. Occurs whenever an environment blocker is lifted and the docs are not revisited                                                                                                                                                                                                                                                                                                                                                                          | 14-08-2026 |
| `README.md` told the reader to run `docker-compose exec api npm run seed`                                                        | Replaced with `npm run seed` on the host, with a comment explaining why the container cannot run it                                                                                                                                            | The seed and job scripts run through `ts-node`, which is a devDependency, and the production image installs with `--omit=dev`. The command could never have worked, and after the image slimming on 13-08-2026 it cannot work by design. Occurs on any attempt to seed from inside the container                                                                                                                                                                                                                                                                                                                                                                       | 14-08-2026 |
| The production image shipped at 830MB carrying `typescript`, `vitest`, `ts-node-dev`, `eslint`, `prettier` and `supertest`       | Added a `production-deps` stage running `npm ci --omit=dev`, and copy `node_modules` from there rather than from `build`                                                                                                                       | The production stage copied `node_modules` wholesale from the build stage, which had installed the full tree. The shortcut existed because `prisma generate` writes the client into `node_modules/.prisma`, so a plain `--omit=dev` install loses it. It is layered back in from the build stage. Occurs on every image build                                                                                                                                                                                                                                                                                                                                          | 13-08-2026 |
| The lockfile was not enforced in the layer that shipped                                                                          | Removed the dead `dependencies` stage. `npm ci` now runs in the two stages whose output is actually consumed                                                                                                                                   | A `dependencies` stage ran `npm ci` and was then referenced by nothing, while `build` ran `npm install`, and production copied from `build`. Occurs on every image build                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | 13-08-2026 |
| The slimmed container exited 1 at boot with `unable to determine transport target for "pino-pretty"`                             | `logger.ts` resolves the module first and falls back to the JSON logger when it is absent                                                                                                                                                      | The logger requested the `pino-pretty` transport whenever `NODE_ENV` is `development`, but `pino-pretty` is a devDependency. Pino resolves the target lazily and throws at import time, killing the process before any route is registered. Hidden until the image stopped shipping dev dependencies, so the logger was always one `npm ci --omit=dev` away from this. Pinning `.env.container` to `production` was rejected as a fix, because it has to stay on `development` for the `DEV_TOKEN` bypass to work                                                                                                                                                      | 13-08-2026 |
| Test files and mocks were compiled into the production image                                                                     | Added `tsconfig.build.json` excluding `src/tests`, and pointed the build script at it                                                                                                                                                          | `tsconfig.json` includes `src/**/*`, so every `.test.ts` was emitted to `dist` and copied in. Nothing requires them at runtime, so it never failed, it just shipped test code. The base config still covers the tests, so editor and typecheck support is unchanged. Occurs on every build                                                                                                                                                                                                                                                                                                                                                                             | 13-08-2026 |
| Six integration tests answered 500 where they expected 200 or 401                                                                | `globalSetup` now defaults `MONGO_URI` with credentials and `authSource=admin`, taking the user and password from `MONGO_ROOT_USER` and `MONGO_ROOT_PASSWORD` with the same fallbacks compose uses                                             | The default was `mongodb://localhost:27017/Studzee_Database_Test`, which carries no credentials, while the compose Mongo runs with `MONGO_INITDB_ROOT_USERNAME` set. Mongoose connects lazily, so setup logged a successful connection and the failure appeared only on the first query as `MongoServerError: Command aggregate requires authentication`. That is why it presented as a server error rather than a connection error. Occurs on every integration run against the compose stack                                                                                                                                                                         | 13-08-2026 |
| An unauthenticated request answered 500 instead of 401                                                                           | Set `CLERK_PUBLISHABLE_KEY` in `globalSetup` to a structurally valid test key, the base64 of a dummy Clerk domain                                                                                                                              | `NODE_ENV` is `test`, so the `DEV_TOKEN` bypass does not apply and the request reaches Clerk, which decodes the publishable key to find its API host and throws `Publishable key not valid` on `pk_test_placeholder`. The throw surfaced as a 500 before `requireAuth` could answer 401. The same root cause makes a container running with the placeholder keys in `.env.container` return 500 for unauthenticated requests                                                                                                                                                                                                                                           | 13-08-2026 |
| A controller test asserted behaviour that should not exist                                                                       | Removed the test and left a comment recording why, pointing at the integration test that covers the guarantee                                                                                                                                  | `should check authentication before fetching document` expected `getDocumentById` to call `req.auth()`. It does not, and should not: `content.route.ts` guards the route with `clerkAuthMiddleware` and `requireAuth`, so anything reaching the controller is already authenticated. Satisfying the test would have meant adding redundant code to the controller                                                                                                                                                                                                                                                                                                      | 13-08-2026 |
| New test files passed at runtime but did not compile                                                                             | Fixed the fixtures to satisfy `ExpoSendResult`, replaced a top level `await import` with `vi.hoisted`, and cast the `NextFunction` mock. `npx tsc --noEmit -p tsconfig.json` is now a CI gate                                                  | Vitest transpiles without typechecking, so a test file can be green and still be wrong. Nothing in the pipeline checked, so this was invisible until typecheck was added. Occurs on any test file written against a partial type                                                                                                                                                                                                                                                                                                                                                                                                                                       | 13-08-2026 |
| `npx vitest run` from the repository root failed every suite with `Cannot find package '@/...'`                                  | Documented in TCSK and RULES that the suite runs from `BACKEND` only                                                                                                                                                                           | The root has no `package.json` and no Vitest config, so npx installed an unrelated Vitest from the registry, no `@/*` alias resolved and `globalSetup` never ran. A root `package.json` was considered and rejected: MOBILE is an Expo app and Metro walks up the tree, so a stray root manifest risks breaking mobile module resolution. Occurs whenever the suite is run from the wrong directory                                                                                                                                                                                                                                                                    | 13-08-2026 |
| The API could not run inside a container at all                                                                                  | Added `BACKEND/.env.container`, which addresses every dependency by its compose service name                                                                                                                                                   | `.env` and `.env.docker` both use `localhost`, which is correct for a host process but resolves to the container itself inside one. `prisma migrate deploy` runs before the app starts, so the container exits 1 with `P1001: Can't reach database server at localhost:5432` and never reaches any application code. Occurs on every attempt to run the image                                                                                                                                                                                                                                                                                                          | 12-08-2026 |
| A containerized API reported `unhealthy` while serving traffic correctly                                                         | Set `PORT=3000` in `.env.container` so the app listens where the image expects, and publish `-p 4000:3000`                                                                                                                                     | The Dockerfile declares `EXPOSE 3000` and its `HEALTHCHECK` probes port 3000, but every env file sets `PORT=4000`, so the probe failed with `ECONNREFUSED` on every interval. An orchestrator using `condition: service_healthy` would block or restart-loop a working service. Occurs on every container start                                                                                                                                                                                                                                                                                                                                                        | 12-08-2026 |
| `make seed`, `make logs` and `make refresh-cache` all fail                                                                       | Documented as broken in the readme with the working npm equivalents alongside                                                                                                                                                                  | They run `docker-compose exec api` or `docker-compose logs -f api`, and there is no `api` service in `docker-compose.yml`. Fails with `service "api" is not running`. Occurs on every invocation                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 12-08-2026 |
| Storage troubleshooting in the readme told the reader to grep for `AWS`                                                          | Rewritten around the failures that actually occur, `NoSuchBucket`, `SignatureDoesNotMatch` and an unreachable public URL                                                                                                                       | The variables were renamed to `S3_*` during the Supabase move, so `cat .env \| grep AWS` returns nothing and the surrounding IAM advice applies to a provider no longer in use                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 12-08-2026 |
| Clerk webhook signature verification could pass or fail by accident                                                              | Mounted the webhook router ahead of `express.json()` with `express.raw`, and verify the exact bytes. The handler now rejects a non Buffer body outright                                                                                        | The handler verified `JSON.stringify(req.body)` after the global JSON parser had already run. Re-serialising does not reproduce the original key order, whitespace or unicode escaping. Occurs on every Clerk delivery                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 10-08-2026 |
| Push broadcast to more than 100 devices failed entirely                                                                          | Chunk messages into batches of 100 in `expo.service.ts`. A failing batch no longer aborts the rest                                                                                                                                             | Every token was posted to Expo in one request. Expo caps a request at 100 messages. Occurs on any broadcast once more than 100 devices are registered                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 10-08-2026 |
| Retired push tokens accumulated and were counted as recipients                                                                   | Read the Expo tickets, collect `DeviceNotRegistered` tokens, and prune them straight after the send                                                                                                                                            | Ticket responses were never inspected and `checkExpoReceipts` was never called. Occurs whenever a user uninstalls the app                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 10-08-2026 |
| Render keepalive never ran in production                                                                                         | Guard now skips only under test and requires `HEALTHCHECK_URL`                                                                                                                                                                                 | The job returned early unless `NODE_ENV === 'test'`, the inverse of the intent. Present in both services. Occurs in every deployed environment                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 10-08-2026 |
| `PUT /admin/documents/:id` rejected every valid payload                                                                          | Validate updates against `DocumentSchema.partial()`                                                                                                                                                                                            | A local schema demanded a string `content` and a `tags` field, neither of which exists on a document. Occurs on every document update                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 10-08-2026 |
| Helmet CSP image directive resolved to the string `undefined`                                                                    | Derive the bucket URL from validated config                                                                                                                                                                                                    | The directive read `process.env.AWS_S3_BUCKET_URL`, a variable defined nowhere. Occurs on every response                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | 10-08-2026 |
| Importing `@/utils/logger` before `@/config` crashed at startup                                                                  | Removed the module scope `logger.info` call from `config/s3.ts`                                                                                                                                                                                | `config/index.ts` re-exports `s3.ts`, which logged at import time, while the logger imports config. Circular import left the logger undefined. Occurs whenever a module reaches the logger before it reaches config                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 10-08-2026 |
| Prisma logged every SQL statement and its parameters in production                                                               | Query logging is development only                                                                                                                                                                                                              | The client was constructed with `log: ['query', ...]` unconditionally. Occurs on every query in every environment                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 10-08-2026 |
| Email attachments could be fetched from any URL                                                                                  | Attachment URLs must be https on a host in `EMAIL_ATTACHMENT_HOSTS`, capped at 10 per message                                                                                                                                                  | Nodemailer fetches an attachment given as a `path`, and the value came straight from the admin request with no checks. Occurs on any email send with attachments                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 10-08-2026 |
| Broadcast emails disclosed the whole recipient list                                                                              | Recipients moved to `bcc`                                                                                                                                                                                                                      | Every address was passed in `to` and joined into one header. Occurs on any multi recipient send                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 10-08-2026 |
| `npm run seed` emptied the database and seeded nothing, reporting success                                                        | Converted the fixture in `src/data/data.json` to the structured section form, and made the script exit non-zero when any document fails validation                                                                                             | The fixture still carried `content` as a single prose string from v1, which `DocumentSchema` rejects because content is structured. The script cleared the collection first, so a run left the database empty while logging "Successfully seeded 0 documents" and exiting 0. Occurs on every seed                                                                                                                                                                                                                                                                                                                                                                      | 11-08-2026 |
| Seed validation errors were logged with no reason attached                                                                       | Pass the context object first, as Pino expects                                                                                                                                                                                                 | The call was `logger.error('Invalid document:', errors)`. Pino takes the object first and the message second, so the errors were silently discarded and every failure printed a bare "Invalid document:"                                                                                                                                                                                                                                                                                                                                                                                                                                                               | 11-08-2026 |
| Local uploads would fail because MinIO had no buckets                                                                            | A one-shot `minio-init` container creates `images`, `pdfs` and `assets` and marks each publicly readable once MinIO is healthy                                                                                                                 | The env files named the buckets but nothing created them, and MinIO creates none on its own. The readme told a developer to create one bucket by hand, which was manual and out of date. Occurs on the first upload in any fresh checkout                                                                                                                                                                                                                                                                                                                                                                                                                              | 11-08-2026 |
| Uploads targeted one bucket with key prefixes, but the project has three buckets                                                 | Split into `S3_BUCKET_IMAGES` and `S3_BUCKET_PDFS`, and made the upload service select a bucket rather than prepend a prefix                                                                                                                   | The Supabase project is laid out as separate `images`, `pdfs` and `assets` buckets. The code wrote `images/<file>` into a single bucket, which does not exist, so every upload would have failed. Found by listing the buckets with the real credentials rather than assuming                                                                                                                                                                                                                                                                                                                                                                                          | 11-08-2026 |
| Object storage was unusable against any non AWS provider                                                                         | `forcePathStyle` and the endpoint are applied unconditionally, and the public URL base is configured through `S3_PUBLIC_URL` rather than derived                                                                                               | `forcePathStyle` and the endpoint were applied only when `NODE_ENV=development`, and the public URL was built in the AWS virtual-hosted form. Supabase and MinIO both address buckets as a path segment and serve public objects from a different host to their S3 endpoint. Occurs on every upload and delete once the provider is Supabase                                                                                                                                                                                                                                                                                                                           | 11-08-2026 |
| `getKeyFromUrl` returned the wrong key for a Supabase URL                                                                        | Strip the configured public base, with a trailing `<folder>/<filename>` fallback for URLs stored under a previous provider                                                                                                                     | It returned the whole URL path, which on Supabase is `storage/v1/object/public/<bucket>/...` rather than the key. Occurs on every image replacement, which deletes the old object by key                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | 11-08-2026 |
| Readiness reported healthy during a Postgres outage                                                                              | Probe MongoDB, Postgres and Redis, and fail the check if any one of them fails                                                                                                                                                                 | The endpoint only checked Mongo and Redis. Postgres arrived with the notification merge and was never added, so every notification endpoint could be failing while a load balancer kept routing traffic                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 10-08-2026 |
| Readiness could report healthy against a dead socket                                                                             | Each probe now issues a real round trip: `admin().ping()`, `SELECT 1` and `PING`, in parallel with a 2 second timeout each                                                                                                                     | The check read `mongoose.connection.readyState` and `redisClient.isOpen`, which report what the driver believes about its socket rather than whether the server answers. Occurs on a network partition or an unresponsive server                                                                                                                                                                                                                                                                                                                                                                                                                                       | 10-08-2026 |
| `KEYS` blocked the Redis event loop during cache invalidation                                                                    | Replaced with a cursor based `SCAN` loop                                                                                                                                                                                                       | `redisClient.keys()` traverses the whole keyspace in one blocking call. Occurs on every admin write                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 10-08-2026 |
| `git commit` failed with `Unable to create .git/index.lock: File exists`                                                         | Confirmed no git process was running, then removed the stale `.git/index.lock` file                                                                                                                                                            | A git process was interrupted on 03-08-2026 and left the lock behind. Occurs on any git write in this repository until the lock is removed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 10-08-2026 |
| Every quiz attempt failed on the deployed Postgres with `Transaction API error: Transaction not found`                           | Moved all seven reads out of the interactive transaction, leaving only the four writes, and raised the budget with `{ timeout: 15000, maxWait: 10000 }`. Streak inputs are now derived by unioning today's key into the pre-read activity rows | Prisma's default 5 second interactive timeout applies to the whole callback. Seven sequential queries against a pooled remote Neon endpoint spent it on latency alone, so the connection closed mid-callback. The unit suite mocked Prisma and could never see it; live verification against the real database caught it on first contact. Occurs on any remote pooled Postgres when an interactive transaction carries more than a couple of round trips                                                                                                                                                                                                              | 25-08-2026 |
